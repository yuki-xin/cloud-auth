<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xin.yuki.auth.core.mapper.PermissionMapper">
    <sql id="Base_Column_List">
               p.code,
               p.name,
               p.description,
               p.id,
               p.additional_information,
               p.version
    </sql>

    <resultMap id="BaseResultMap" type="xin.yuki.auth.core.entity.PermissionModel">
        <result column="code" property="code"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="id" property="id"/>
        <result column="version" property="version"/>
        <result column="additional_information" property="additionalInformation"
                typeHandler="xin.yuki.auth.core.type.handler.JSONTypeHandler"/>
    </resultMap>

    <!--auto generated by codehelper on 2018-12-24-->
    <select id="findByRoleId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from permissions p
        left join roles_permissions rp on rp.permission_id=p.id
        left join roles r on rp.role_id=r.id
        where r.id=#{roleId}
    </select>

</mapper>
