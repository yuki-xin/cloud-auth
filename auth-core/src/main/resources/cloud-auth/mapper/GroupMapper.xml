<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="xin.yuki.auth.core.mapper.GroupMapper">
    <sql id="Base_Column_List">
               g.name,
               g.description,
               g.id,
               g.version,
               g.additional_information
    </sql>

    <resultMap id="BaseResultMap" type="xin.yuki.auth.core.entity.GroupModel">
        <result column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="version" property="version"/>
        <result column="additional_information" property="additionalInformation"
                typeHandler="xin.yuki.auth.core.type.handler.JSONTypeHandler"/>
    </resultMap>


    <!--auto generated by codehelper on 2018-12-24-->
    <select id="findByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from groups g
        left join users_groups ug on g.id=ug.group_id
        left join users u on u.id=ug.user_id
        where u.id=#{userId}
    </select>



</mapper>
